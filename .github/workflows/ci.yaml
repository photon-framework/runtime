name: CI
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 17
          cache: "yarn"
          registry-url: "https://registry.npmjs.org"
      - run: yarn
      - run: yarn build-core
      - run: yarn rename-core
      - run: yarn build-tsc
      - uses: JS-DevTools/npm-publish@v1
        with:
          token: ${{secrets.NPM_TOKEN}}
          access: public
          check-version: true
